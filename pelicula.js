'use strict'
var nombre = function(nombre) {
    return "La pelicula que estas apunto de ver es: " + nombre;
}
nombre("Star Wars")

var pelicula = function(nombre) {
    return "La pelicula que estas apunto de ver es: " + nombre;
}

function sumar(numero1, numero2) {
    var suma = numero1 + numero2;
}
sumar(3, 5);

document.write("Honestamente la 6 fue la mejor");